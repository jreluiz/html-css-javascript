/*
 * EXERCÍCIO 3: Gerador de Tabuada Personalizada
 * 
 * Crie um sistema de tabuada com as seguintes funcionalidades:
 * 
 * 1. Crie uma função 'gerarTabuada' que recebe dois parâmetros:
 *    - numero: o número da tabuada a ser gerada
 *    - limite: até qual número multiplicar (padrão 10)
 * 
 * 2. A função deve usar um loop for para gerar a tabuada e
 *    armazenar os resultados em um array de objetos, onde cada
 *    objeto tem:
 *    - multiplicador
 *    - resultado
 * 
 * 3. Crie uma função 'filtrarResultadosPares' que recebe o array
 *    da tabuada e retorna apenas os resultados pares usando um loop
 * 
 * 4. Use if/else para verificar se o número informado é válido
 *    (deve ser maior que 0 e menor que 100)
 * 
 * 5. Imprima a tabuada completa e depois apenas os resultados pares
 */

// Seu código aqui:

function gerarTabuada(numero, limite = 10) {
    // Valide se o número é válido
    
    // Gere a tabuada usando for
    
}

function filtrarResultadosPares(tabuada) {
    // Implemente aqui
}

// Teste com os seguintes números:
console.log('=== TABUADA DO 7 ===')
// Gere e imprima a tabuada do 7

console.log('\n=== APENAS RESULTADOS PARES ===')
// Filtre e imprima apenas os pares

// Teste com número inválido:
console.log('\n=== TESTE COM NÚMERO INVÁLIDO ===')
// Teste com o número 0 ou 150
