/*
 * EXERCÍCIO 1: Calculadora de Média de Notas
 * 
 * Crie uma função chamada 'calcularMedia' que recebe um array de notas
 * e retorna a média aritmética dessas notas.
 * 
 * Depois, crie uma função chamada 'verificarAprovacao' que recebe a média
 * e retorna uma string usando operador ternário:
 * - "Aprovado" se a média for >= 7
 * - "Recuperação" se a média for >= 5 e < 7
 * - "Reprovado" se a média for < 5
 * 
 * Por fim, crie um objeto 'aluno' com as propriedades:
 * - nome
 * - notas (array)
 * - media (calculada)
 * - situacao (resultado da aprovação)
 * 
 * Imprima o resultado final no console.
 */

// Seu código aqui:

function calcularMedia(notas) {
    // Implemente aqui
}

function verificarAprovacao(media) {
    // Implemente aqui
}

// Teste com os seguintes dados:
const aluno = {
    nome: 'João Silva',
    notas: [7.5, 8.0, 6.5, 9.0]
}

// Complete o código para calcular média e situação
